Windows 10 Setup 


CTRL + SHIFT + F3

msdos cmd window

sysprep.exe /oobe /reboot /unattend:D:\relocate.xml


AMPPS:

sudo QT_X11_NO_MITSHM=1 /usr/local/ampps/Ampps


if MAPPS does not start:

usr/local/ampps/apache/lib

#sudo mkdir backup
#sudo mv ./libapr* ./backup/

sudo rm -rf ./libapr*
sudo apt-get -y install libaprutil1 libaprutil1-dev libapr1 libapr1-dev 


PORTS IN USE:
sudo lsof -i -P -n | grep LISTEN 
sudo lsof -i:80 